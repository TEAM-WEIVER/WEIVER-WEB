# US 리뷰 — #44 Round 1

## 판정: PASS

## Critical

없음.

## Major

- [ ] AC3: `ANSWER_ACCEPTED` 수신 없이 바로 `INTERVIEW_FINISHED`가 오는 경우(마지막 답변 제출 직후 종료) 흐름이 AC에 명시되지 않음. 상태 머신 다이어그램에는 `SUBMITTING → INTERVIEW_FINISHED` 전이가 있으나 AC3/AC4 어디에도 이 경우를 명시하는 Given/When/Then이 없어 테스트 작성 불가.
- [ ] AC2: 질문 수신 후 음성 입력 UI 활성화가 언급되나, 음성 입력 관련 AC(권한 요청 거부, 음성 인식 실패 등)가 전혀 없음. 구현 범위에 포함된다면 별도 AC 필요, 제외라면 "텍스트 입력만 지원" 명시 필요.
- [ ] AC6: 재연결 후 세션 복구 가능 여부가 백엔드 협의 사항으로만 남겨져 있어 재연결 성공 시 동작(이전 질문 재표시 vs 새 질문 수신 대기)이 AC에 정의되지 않음. 재연결 성공 경로 Then이 누락.

## Minor

- [ ] AC1 Then에 상태 전이 순서(CONNECT → SUBSCRIBE → START)가 기술되어 있으나 이는 구현 세부사항에 가까움. UI 관점 Then과 분리하면 테스트 시 관심사 혼재 방지.
- [ ] AC5: 인증 오류(401)와 네트워크 이상(연결 자체 실패)이 하나의 AC에 묶여 있음. 두 오류의 Then 분기(리다이렉트 vs 에러 메시지만)가 다르므로 AC를 분리하면 가독성 향상.
- [ ] 상태 열거형에 `RECONNECTING` 상태가 없으나 AC6에서 재연결 중 UI가 별도로 존재함. 상태와 UI 간 일관성 확인 필요.
