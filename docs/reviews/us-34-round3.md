# US 리뷰 — #34 Round 3

## 판정: PASS (조건부)

> Gemini R3 PASS (confidence 0.98) / Codex R3 FAIL 의견 충돌.
> Codex 제기 항목을 문서 원문 기준으로 재판정한 결과, Critical 해당 항목 없음.
> 단, Codex의 Major 항목 중 구현 전 확인이 필요한 사항이 다수 포함되어 있어 개발 착수 전 하기 Major 항목을 팀 내 확인 후 진행 권고.

---

## Critical

_해당 없음 — R1/R2 지적사항 모두 해소 확인(Gemini), Codex 제기 C1~C4는 재판정 결과 Major 이하로 하향_

---

## Major

- [x] **[라우팅 흐름 미명시]** AC1(`/onboarding` gate)에서 PIPA 동의 완료 후 `/onboarding/resume`으로 이동하는 명시적 Then 절 없음. 동의 후 라우팅 동작이 AC에 기술되지 않아 구현자가 임의 해석 가능 (Codex C2 재분류) → **해소**: AC1 Then에 `/onboarding/resume` 이동 명시, 전제 조건 섹션에 진입 경로(신규 가입/대시보드 재진입) 기술
- [x] **[AC3 재시도 중복 POST]** 세부 항목(education/experience 등) API 부분 실패 후 재시도 시 이미 성공한 항목에 대해 snapshot POST가 재호출되어 중복 저장될 수 있음. 재시도 범위(전체 재전송 vs 실패 항목만)를 AC3에 명시 필요 (Codex C3) → **해소**: 전체 재전송으로 명시, 에러 시 토스트로 새로고침 안내
- [x] **[GET 실패 케이스 누락]** AC6/AC10에서 `GET /api/essay-answers`, `GET /api/portfolios` 호출 실패(네트워크 오류, 5xx) 시 동작이 정의되지 않음 (Codex M7) → **해소**: GET 실패 시 토스트로 새로고침 안내, 페이지 이동 없음 명시
- [x] **[파일 에러 후 제출 가능 여부 미구분]** AC11에서 파일 에러 발생 시 "제출 버튼 비활성 유지"로 정의되어 있으나, 에러 파일을 제거한 후 제출 가능 여부가 AC10·AC11 간에 상충 (Codex M8) → **해소**: 제출 버튼 활성화 조건을 "유효한 파일 첨부 + 개인정보 동의 체크" 두 조건 모두 충족으로 명시
- [x] **[skip + 대시보드 보호 라우팅 충돌 가능성]** AC5/AC9/AC13에서 "나중에 작성" 후 대시보드 접근 시, 온보딩 미완료 상태 지원자의 대시보드 접근을 허용하는지 차단하는지 정의 없음. AC15의 인증 미완료 리다이렉트 정책과 충돌 가능 (Codex M9) → **해소**: 전제 조건에 "온보딩 미완료 상태에서도 대시보드 접근 허용, 차단하지 않음" 명시
- [x] **[AC1 PIPA 동의 서버 저장 여부 불명확]** PIPA 동의 체크 후 서버에 저장하는 API가 AC 및 API 연동 테이블에 없음. 클라이언트 상태만 관리하는지 서버 기록이 필요한지 미정의 (Codex M10) → **해소**: 서버 저장 API 존재 확인됨

---

## Minor

- [ ] AC6 `answer` 포맷(구분자 vs JSON 직렬화)이 "백엔드 협의 필요"로 열려 있어 표현 모호 — 구현 전 확정 권고 (Codex M5, 문서 내 협의 필요 항목 #1로 이미 기재됨)
- [ ] `portfolioId` 없이 포트폴리오 제출 가능 여부가 "백엔드 협의 필요"로 열려 있음 — 문서 내 협의 필요 항목 #3으로 이미 기재, 표현 중복 (Codex M6)
- [ ] Codex가 "문서 버전 불일치(C1)"로 제기했으나 문서 내 버전 정보 자체가 없어 검토 불가 — 별도 확인 불필요로 판단
